(ns com.howard.uchat.backend.socket.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.howard.uchat.backend.socket.interface :as socket]))

(deftest dummy-test
  (is (= 1 1)))
