(ns com.howard.uchat.backend.messages.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.howard.uchat.backend.messages.interface :as messages]))

(deftest dummy-test
  (is (= 1 1)))
