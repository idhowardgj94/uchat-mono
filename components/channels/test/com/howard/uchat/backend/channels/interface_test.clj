(ns com.howard.uchat.backend.channels.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.howard.uchat.backend.channels.interface :as channels]))

(deftest dummy-test
  (is (= 1 1)))
