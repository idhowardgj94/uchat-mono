(ns com.howard.uchat.backend.teams.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.howard.uchat.backend.teams.interface :as teams]))

(deftest dummy-test
  (is (= 1 1)))
