(ns com.howard.uchat.backend.database.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.howard.uchat.backend.database.interface :as database]))

(deftest dummy-test
  (is (= 1 1)))
