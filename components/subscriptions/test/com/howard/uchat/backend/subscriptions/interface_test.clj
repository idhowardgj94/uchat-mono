(ns com.howard.uchat.backend.subscriptions.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.howard.uchat.backend.subscriptions.interface :as subscriptions]))

(deftest dummy-test
  (is (= 1 1)))
