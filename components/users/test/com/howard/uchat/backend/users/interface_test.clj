(ns com.howard.uchat.backend.users.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.howard.uchat.backend.users.interface :as users]))

(deftest dummy-test
  (is (= 1 1)))
