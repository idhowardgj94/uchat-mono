(ns com.howard.uchat.backend.tools.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.howard.uchat.backend.tools.interface :as tools]))

(deftest dummy-test
  (is (= 1 1)))
